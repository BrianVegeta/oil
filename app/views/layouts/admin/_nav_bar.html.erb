<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%= t('admin.nav.header.name') %></a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <% if current_user.is_product_manager %>
          <li class="active">
            <a href="<%= admin_products_path %>">
              <%= t('admin.nav.link_list.manage_product') %>
            </a>
          </li>
        <% end %>

        <% if current_user.is_customer_service_manager %>
          <li>
            <a href="#"><%= t('admin.nav.link_list.manage_customer_service') %></a>
          </li>
        <% end %>

        <% if current_user.is_super_admin %>
          <li>
            <a href="<%= admin_users_path %>">
              <%= t('admin.nav.link_list.manage_manager') %>
            </a>
          </li>
        <% end %>

      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <%= current_user.email %>
            （<%= current_user.username %>）
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <!--
            <li><a href="#">Action</a></li>
            <li><a href="#">Another action</a></li>
            <li><a href="#">Something else here</a></li>
            <li class="divider"></li>
            -->
            <li>
              <%= link_to t('admin.nav.user.logout'), destroy_user_session_path, data:{method: :delete} %>
            </li>
          </ul>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>